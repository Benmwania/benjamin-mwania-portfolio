[build]
  command = "npm run build"
  publish = ".next"

[build.environment]
  NODE_VERSION = "20"

# Disable the automatic Next.js plugin
[[plugins]]
  package = "@netlify/plugin-nextjs"
  enabled = false